let global = 10;

let sign = fn(v: u32): u32;

let nothing = fn(v: u32): u32 { v };

let f = fn(
    a: u32,
    b: u32,
): u32 {
    a + b
};

let func = fn(b: bool): u32 {
    if b {
        return 4;
    };

    5
};

let main = fn() {
    comptime_print(func(false));
    return;
    
    // let T = u32;
    // let mut arr: [4]T = undefined;
    // arr[0] = 4;
    // printi(arr[0]);

    // let v = {
    //     let x = 5;
    //     x + x
    // };

    // // $
    // // let error = unknown;
    // // let n: u32 = "hello"

    // let range = 4..5;
    // let slice: []T = &arr;
    // let elem: *mut T = &arr[0];
    // elem.* = 5;
    // printi(arr[0]);

    // let ptr = malloc(1);
    // ptr.* = 6;
    // printi(ptr.*);

    // // slice[0] = 5

    // "hello";
    
    //     let a = 5;
    //     let b =  6;
    //         print("hello"); nothing(a + b + global);

    // let printf = extern fn(s: *u8, ..): c_int @ "printf";
    // srand();
    // let num = rand();
    // printf("arr[0]: %lu\n\x00".ptr, arr[0]);
    // printf("num: %lu\n\x00".ptr, num);
    let mut a = 0 as u64;
    let mut b = 1 as u64;
    loop {
        comptime_print(a);
        // printf("%u\n\x00".ptr, a);
        let tmp = a + b;
        a = b;
        b = tmp;
        // comptime_sleep(1);
        // sleep(1);

        if a >= 100000 as u64 {
            // comptime_print("done");
            break;
        } else {
            // comptime_print("continue");
        };
    };
    // print("done");
    // let c = 4;
};
