// let global = 10;

// let sign = fn(v: u32): u32;

// let nothing = fn(v: u32): u32 { v };

let f = fn(
    a: u32,
    b: u32
): u32 {
    a + b
};

let main = fn() {
    f(5, 10)
    // f(nothing(5), nothing(7))
};
